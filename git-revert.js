/**
 
- git reset (compare diff ở giữa 2 commit)
-> a - b - c - d
=> reset từ d->a
=> diff = b,c

- git revert (compare change tại current commit vs commit đứng liền trước commit đang destinate <tức là không so sánh với change trong commit destinate, mà so sánh với các change trước khi commit destinate được commit>)
 
*/