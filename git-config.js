/**
 * @_GIT_CONFIG
 * 
 * @Description
 * !Git log is used for case want to change account git used
 * 
 * - git config --list: Xem cấu hình git trên local
 * - git config user.name: Xem thông tin username
 * - git config user.email: Xem thông tin email
 * 
 * todo: Thay đổi thông tin tài khoản git như sau:
 * - git config --global user.name "your-username"
 * - git config --global user.email "your-email@gmail.com"
 * 
 * ? Một file: <~/.gitconfig> trên device sẽ chứa các thông tin cấu hình git
 */